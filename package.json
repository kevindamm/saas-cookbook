{
  "name": "edge-saas",
  "version": "0.7",
  "description": "Libraries and Utilities for hosting a SaaS on edge compute with Cloudflare.",
  "scripts": {
    "type-check": "wrangler types --experimental-include-runtime && tsc",
    "dev": "wrangler dev",
    "test": "vitest",
    "deploy": "wrangler deploy --minify",
    "book:dev": "vitepress dev book",
    "book:build": "vitepress build book",
    "book:preview": "vitepress preview book"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kevindamm/edge-saas.git"
  },
  "keywords": [
    "cloudflare",
    "chat",
    "workers",
    "pages",
    "durable objects",
    "R2", "D1", "websockets"
  ],
  "author": "Kevin Damm",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.40",
    "@cloudflare/workers-types": "^4.20241224.0",
    "@tsconfig/strictest": "^2.0.5",
    "typescript": "^5.5.2",
    "vitepress": "^1.5.0",
    "vitest": "2.1.8",
    "wrangler": "^3.99.0"
  },
  "engines": {
    "node": ">=18.20.0"
  },
  "dependencies": {
    "hono": "^4.6.15"
  }
}
